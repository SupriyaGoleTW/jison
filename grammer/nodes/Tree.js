var Tree = function (treeValues) {
    this.value = treeValues;
    this.type = 'tree';
    this.evaluate = evaluate;
};

var evaluate = function (allNodes) {
    //process all nodes
};

module.exports = Tree;
